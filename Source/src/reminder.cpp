#include "../headers/reminder.h"
#include <QString>

/*
 * Sets the todo id, the message and the title of the reminder
*/
Reminder::Reminder(int id,QString msg,QString ttl){
    this->todoId=id;
    this->message=msg;
    this->title=title;
}
